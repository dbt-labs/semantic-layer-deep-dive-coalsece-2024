semantic_models:
  - name: customers
    description: customer record book, grain is one customer per row 
    model: ref('customers')
    defaults:
      agg_time_dimension: first_ordered_at
    entities:
      - name: customer
        type: primary
        expr: customer_id
    dimensions:
      - name: customer_name
        type: categorical
      - name: customer_type
        type: categorical
      - name: first_ordered_at
        type: time
        type_params:
          time_granularity: day
      - name: last_ordered_at
        type: time
        type_params:
          time_granularity: day
    measures:
      - name: customers
        description: Count of our unique customers
        agg: count_distinct
        expr: customer_name
      - name: count_lifetime_orders
        description: Total count of orders per customer
        agg: sum
      - name: lifetime_spend_pretax
        description: Customer lifetime spend before taxes
        agg: sum
      - name: lifetime_spend
        description: Gross customer lifetime spend inclusive of taxes
        agg: sum
metrics: 
  # Simple type metrics
  - name: customers
    description: count of customers
    type: simple
    label: Customer Count
    type_params:
      measure:
        name: customers
  - name: count_lifetime_orders
    description: lifetime order count
    type: simple
    label: Lifetime Orders
    type_params:
      measure:
        name: count_lifetime_orders
  - name: lifetime_spend_pretax
    description: total amount of spend before tax
    type: simple
    label: Lifetime Spend Pretax
    type_params:
      measure:
        name: lifetime_spend_pretax
  # # Ratio type metric
  # - name: "avg_order_value"
  #   label: "avg_order_value"
  #   description: "average value of each order"
  #   type: ratio
  #   type_params:
  #     numerator: order_total
  #     denominator: order_count
  # # Cumulative type metrics
  # - name: "cumulative_order_amount_mtd"
  #   label: "cumulative_order_amount_mtd"
  #   description: "The month to date value of all orders"
  #   type: cumulative
  #   type_params:
  #     measure:
  #       name: order_total
  #     grain_to_date: month
  # # Derived metric
  # - name: "pct_of_orders_that_are_large"
  #   label: "pct_of_orders_that_are_large"
  #   description: "percent of orders that are large"
  #   type: derived
  #   type_params:
  #     expr: large_orders/order_count
  #     metrics:
  #       - name: large_orders
  #       - name: order_count